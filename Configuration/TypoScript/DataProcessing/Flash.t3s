dataProcessing.flash = Ps\Xo\DataProcessing\ContentProcessor
dataProcessing.flash {
	table = tt_content
	as = flash
	select {
		where.data = field:uid
	}
}

dataProcessing.flashPage < dataProcessing.flash
dataProcessing.flashPage {
	select.where.wrap = colPos = 326022 AND tx_xo_page = |
}

dataProcessing.flashContent < dataProcessing.flash
dataProcessing.flashContent {
	select.where.wrap = colPos = 326022 AND tx_xo_parent = |
}